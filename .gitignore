# 依赖 / 环境 / 编辑器
node_modules/
.env
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# 临时工作目录
_temp/
pw-browsers/

# 运行时事实文件(含项目内部信息,不随 skill 分发)
system-facts.md
product-facts.md

# 生成图片:默认忽略,只保留文档引用到的(让别人拉取更轻)
*.png

# 例外:README/文档引用的展示媒体
!assets/readme/
!assets/readme/**

# zip 残留
*.zip
zi??????

# 测试产物
test/_out/

# 候选社区库(pick 临时区,不入库)
_candidates/
excalidraw-diagram-skill

# eval 运行产物(巨大 150M / 可重生；密钥在用户 ~/.config/opencode，不在本仓库)
eval/out/
eval/node_modules/
eval/report-*.html

# 各类型本地测试渲染产物(可重生)
examples/*/out/

# 一次性脚本产物 / 临时
/out/
tmp-*.mmd
/tmp/

# 组件参考表 PNG 需入库(被 prompts/*.md 引用)，覆盖上面 *.png 的全局忽略
!assets/mermaid-components/
!assets/mermaid-components/**
